# Todo Application with React Context API
A Todo application built with React(Vite) that demonstrates state management using the Context API. The application supports todo management, filtering, theme switching, and persistence using localStorage.

